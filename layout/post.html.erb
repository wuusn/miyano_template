<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="created" content=<%= cre_date.to_s.inspect %> />
    <meta name="modified" content=<%= mod_date.to_s.inspect %> />
    <meta name="tags" content=<%= tags.join(", ").inspect %>/>
    <link rel="stylesheet" href="/default.css" type="text/css">
  </head>
  <body>
    <main class="note-wrapper">
      <!-- <nav>
        <a href="/">#</a>
        <% tags.each do |tag| %>
          <a href="/tag/<%= tag %>/"># <%= tag %></a>
        <% end %>
      </nav> -->
      <article>
       <%= content %>
     </article>
    </main>
  </body>
<html>